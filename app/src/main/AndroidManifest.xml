<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools"
    package="com.mlambo.smarthike">

    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme"
        tools:ignore="GoogleAppIndexingWarning">
        <activity
            android:name=".Home.PassengerActivity"
            tools:ignore="InnerclassSeparator" />
        <activity

            android:name=".Home.DriveActivity"
            tools:ignore="InnerclassSeparator" />


        <activity android:name=".Authentication.PasangerSignUp"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".Authentication.DriversSignUp"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".Walkthrough.Welcome"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".Walkthrough.Walkthrough"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".Authentication.Profile"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".Authentication.PhoneAuth"
            tools:ignore="InnerclassSeparator" />

        <activity android:name=".Home.MainActivity"
            tools:ignore="InnerclassSeparator">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>